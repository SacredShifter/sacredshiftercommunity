import React from 'react';
import { AuraConversation } from '@/components/AuraConversation';

export function AuraInterface() {
  return (
    <div className="space-y-6">
      {/* Clean single conversation interface */}
      <AuraConversation />
    </div>
  );
}